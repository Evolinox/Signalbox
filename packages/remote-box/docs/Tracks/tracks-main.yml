info:
  name: tracks/main
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:9074/tracks/main
  body:
    type: json
    data: |-
      {
        "power": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
