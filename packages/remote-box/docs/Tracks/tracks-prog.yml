info:
  name: tracks/prog
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:9074/tracks/prog
  body:
    type: json
    data: |-
      {
        "power": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
